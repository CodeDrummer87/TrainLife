spring:
  application:
    name: trainlife

  datasource:
    url: jdbc:postgresql://localhost:5434/trainlife_db
    username: postgres
    password: postgres

  flyway:
    enabled: true
    locations: classpath:db.migration

  jpa:
    hibernate:
      ddl-auto: update
